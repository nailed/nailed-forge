
local native = (map.native and map.native()) or map
local map = {}

local function wrap(_sFunction)
	return function(...)
		return native[_sFunction](...)
	end
end

map.countdown = function(seconds, message)
    local ellapsed = 0
    repeat
        local out, num = string.gsub(message, "%%s", (seconds - ellapsed) .. "")
        map.sendTimeUpdate("" .. out)
        sleep(1)
        ellapsed = ellapsed + 1
    until ellapsed >= seconds
end

map.native = function()
    return native
end

for k,v in pairs(native) do
	if type(k) == "string" and type(v) == "function" then
		if map[k] == nil then
			map[k] = wrap(k)
		end
	end
end

local env = getfenv()
for k,v in pairs(map) do
	env[k] = v
end
