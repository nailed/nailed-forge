if(project.hasProperty("deploymaven")){
    nailedCredentials {
        deployServer = "maven.reening.nl"
        deployUsername = project.deploymaven.username
        deployPassword = project.deploymaven.password
        versionFile = "nailed/versions-1.json"
    }
}
